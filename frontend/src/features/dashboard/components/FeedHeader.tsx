import PageTitle from '@/features/dashboard/components/common/PageTitle';

export default function FeedHeader() {
  return <PageTitle eyebrow="Live Feed" title="Every tremor, as it happens." subtitle="Scan, filter, and export global seismic activity the moment it's detected." />;
}


